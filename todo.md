# 용머리성당 홈페이지 작업 기록 (todo.md)

> 마지막 정리: 2026-09-26 (Cloudflare Workers + D1 전환 반영)
> git 커밋 로그 기준으로 정리한 내용입니다. 새로 작업할 때마다 이 파일을 업데이트해주세요.
> 새 주소: https://yongmeori-catholic.jangsangyun0310.workers.dev/ (홈페이지·관리자 한 주소, 구조·설정은 `docs/관리자-설정-안내.md`)

## 지금까지 완료한 작업

### 0. Cloudflare Workers + D1 전환 — 2026-09-26
- **배경(사용자 결정):** 네 프로젝트를 운영 전에 결제 없는 반영구 구조로 통일. 서학동성당(project2)을 먼저 옮겨 기준으로 삼고 같은 방식으로 용머리성당을 옮김 (다음: 해월피정의집·samudaejeon 공통 부분)
- **주소 통합:** 홈페이지(GitHub Pages `github.io`)와 관리자(Cloudflare Pages `pages.dev`)로 나뉘어 있던 것을 Worker 한 주소로 합침 → 로그인 상태가 이어지고 `ADMIN_URL` 불필요. 공유 주소(CANONICAL_URL)·링크 미리보기(og) 주소도 새 주소로
- 사이트 파일을 `public/`으로 이동, 서버 `src/`(서학동과 같은 코드 + 본당 일정 + 공지 날짜 문구 허용), 표 구조 `migrations/0001_initial.sql` (서학동 표 + `schedule`)
- 공지 날짜 칸은 "상시 모집", "접수마감 2026.09.20" 같은 문구도 허용하고, 목록은 추가한 순서(최근 것이 위) — 예전과 같은 동작
- 홈페이지는 기존 화면 코드를 그대로 두고 `loadContent()`만 서버 데이터(`/api/home`, `site-data.js`)를 예전 JSON 모양으로 돌려주게 바꿈. 갤러리는 대표 사진·장수만 받고 앨범을 열 때 사진 목록 요청. 알림함·구독은 Firestore 대신 서버. 홈페이지에서 Firestore·Firebase 로그인 SDK 제거
- **오늘의 말씀:** 매일 `index.html`을 고쳐 커밋(=매일 재배포)하던 방식 → GitHub Actions가 서버 호출, 서버가 D1에 저장. 실패 메일은 그대로
- **예전 주보 222장:** 다음카페 서버(t1.daumcdn.net)에서 가져다 쓰던 것을 사용자 결정으로 우리 저장소로 옮김 — 받아서 긴 변 2000px로 줄여 95.6MB → 50.8MB, `public/images/uploads/연도/bulletin-날짜-면.jpg`. 다음카페 글이 지워져도 주보 유지
- 카카오 지도(용머리 + 전주 성당 30여 곳)는 그대로 유지하고, 아래에 네이버·카카오·구글 지도 열기 버튼 추가(서학동과 통일, 카카오 지도가 안 뜰 때도 사용 가능)
- 기존 데이터 전부 D1로 이전: 공지 3, 주보 111(222면), 앨범 262(사진 541), 본당 일정 9, 팝업, 알림함 4, 구독자 7, 오늘의 말씀
- Firebase 승인 도메인에 새 주소 추가, Worker 비밀값 `FIREBASE_SERVICE_ACCOUNT`·`CRON_SECRET`, GitHub Actions 비밀값 `CRON_SECRET` 설정
- 테스트: `npm test` 14개(서학동 13개 + 본당 일정, 공지 날짜 문구 포함) 통과, `npm run test:browser`(홈페이지 공지·주보·앨범·일정 달력·알림함, 관리자 공지·일정·갤러리·명단) 통과. 실제 새 주소에서도 데이터·오류 없음 확인

### 1. 홈화면 설치 / PWA
- 카카오톡 인앱 브라우저에서 크롬으로 강제 이동하는 딥링크 체인 구현 (카카오톡 → 삼성인터넷 → 크롬)
- 홈화면 추가 로직·공유하기 링크를 서학동성당(project2) 원본과 동일한 단순 방식으로 통일
- 홈 화면 아이콘(icon-192/512, 애플 터치 아이콘) 상단 잘림 수정, 엠블럼 크기 69%로 재조정
- **주의(메모리):** 운영 중인 프로덕션 URL을 바꾸면 이미 설치된 사용자의 원탭 PWA 설치가 깨지므로, URL 변경은 신중히.

### 2. 알림(푸시) 기능
- Firebase Cloud Messaging 기반 웹 푸시 알림 추가 (서학동성당 참고)
- VAPID 키 반영, Firebase 프로젝트(yongmeori-church) 설정 반영
- 이전 알림 토큰 정리 로직 추가
- 알림 받기 버튼을 별도 버튼 없이 설치 흐름에 자연스럽게 통합
- 알림함 종 버튼을 본당 소개 왼쪽 내비 메뉴 안으로 이동

### 3. 관리자 페이지
- 구글 로그인 후 공지·주보·갤러리·본당 일정·팝업·알림을 홈페이지에서 직접 관리할 수 있는 관리자 페이지(`admin/index.html`) 추가
- 관리자 버튼을 오른쪽 아래 고정 버튼으로, 연락처는 푸터로 이동
- 이후 본당 일정 여러 차례 관리자 페이지에서 수정

### 4. 본당 일정 & "전체 일정 보기"
- 1박2일 등 여러 날짜에 걸친 일정 입력 지원
- "전체 일정 보기" 추가: 달력 칸 안에 내용까지 보이는 한 달 전체보기 + 인쇄 기능
- 여러 날짜 걸친 일정 표시를 화살표 → `(~16)` 같은 짧은 표시로 단순화
- **인쇄(전체 일정) 관련 반복 개선** (가장 공들인 부분):
  - A4 한 장에 항상 꼭 맞는 고정 크기 달력으로 재설계
  - 화면과 인쇄 결과가 다르게 잘리던 문제 수정 (칸 높이 강제, 2줄 자르기 제거)
  - 잉크 절약보다 "화면과 똑같이 보이게" 우선순위로 재작업
  - 흰 여백에 크림색 배경이 비치던 문제 제거, A4 꽉 채우기 복원
  - 칸 크기를 실제로 고정하고, 내용이 넘치면 글자 크기를 줄이도록 수정
  - 원래 색깔로 복원
  - **관련 메모리:** `print-css-chrome-gotchas` — 크롬 인쇄 CSS 관련 노하우가 정리되어 있음. 인쇄 기능을 서학동성당(project2)에 이식할 때도 참고할 것.

### 5. 오늘의 말씀
- 자동 갱신 스크립트로 매일 갱신 (2026.09.10 ~ 09.16까지 지속적으로 커밋됨)
- 정규식 보완: 복음 구절 조사 '이/가' 모두 매칭되도록 수정
- 성서 구절이 길 때 잘리지 않고 아래 줄로 내려가도록 수정 (최신 커밋)

### 6. 공지 / 팝업
- 접속 시 공지·행사 안내 팝업 추가
- 추석 합동위령미사 안내를 텍스트 대신 디자인된 이미지로 교체, 일정 등록
- 사무장 채용 공지 마감일을 일정과 동기화

### 7. UI/UX 다듬기
- PC 상단 메뉴 줄바꿈 수정, 중복 "맨 위로" 버튼 제거
- 모바일 헤더 글씨크기·버튼 간격 정리, 로고 찌그러짐 수정
- 모바일 가로 스크롤(움직임) 방지
- 글씨 크게 보기 버튼 추가
- 로고 클릭 시 맨 위 이동 + 새로고침
- 미사시간 표기(띄어쓰기 등) 정리
- 푸터 연락처 목록 정리 (불필요한 문구·구분선 제거)
- **원칙(메모리):** 노인 이용자 대상 사이트이므로, 다른 기술적 개선보다 "쉬운 사용성"을 항상 우선한다.

### 8. 기타
- GitHub Pages 404 수정 (파일명 `index.html`로 변경)
- og:url 등 하드코딩된 옛 주소/저장소 이름 정리

### 9. 환영 + 바로가기 설치 안내 팝업 (2026-09-25)
- 신자들에게 홈페이지 링크를 처음 전달할 때, 접속하자마자 한 번만 뜨는 "용머리성당 홈페이지에 오신 것을 환영합니다" 팝업(`welcomeInstallModal`) 추가
- 큰 "설치하기" 버튼과 "다음에 할게요" 버튼만 있는 단순한 구성 (어르신 우선 원칙 반영)
- "설치하기"를 누르면 기존 오른쪽 아래 설치 버튼과 완전히 같은 로직(`triggerInstall`을 `window.__triggerInstall`로 공유)을 타서, 기기별(아이폰/안드로이드/카카오톡 인앱) 단계별 안내(`installModal`)로 자연스럽게 이어짐
- 이미 설치(standalone)된 사용자에게는 뜨지 않고, `localStorage`(`yongmeori-welcome-install-shown`)로 한 번만 노출
- 오늘의 공지 팝업(`announceModal`)이 떠 있는 경우 겹치지 않도록, 그 팝업이 닫힌 뒤에 띄우도록 처리
- Playwright로 로컬 정적 서버 기동 후 모바일(안드로이드) UA로 팝업 노출 → 설치 버튼 클릭 → 안드로이드 설치 안내까지 전체 흐름 스크린샷으로 확인 완료

### 10. 오늘의 말씀 자동 갱신 실패 수정 (2026-09-26)
- 복음 인용이 `9,43ㄴ-45`처럼 한글 자모로 절을 세분화하는 날 정규식이 매칭에 실패해 자동 갱신이 멈추고 실패 메일이 왔음 → 인용 부분에 자모(ㄱ-ㅎ) 허용하도록 `.github/scripts/update-todayword.js` 수정, 9/26 값 반영. 서학동성당(project2)도 동일하게 수정.

### 11. 알림함 안 "알림 받기" 안내창이 알림함 뒤에 깔리던 문제 수정 (2026-09-26)
- 아이폰(설치 전)·알림 차단·카카오톡 인앱에서 알림함 안의 "알림 받기"를 누르면 뜨는 안내창(`pushModal`)이 알림함(`notifPanel`)보다 HTML 앞쪽에 있어서 뒤에 가려져, 눌러도 반응이 없는 것처럼 보였음
- `pushModal`을 `notifPanel` 뒤로 옮겨 위에 겹쳐 보이도록 수정, 안내 문구도 "알림함(종 버튼)에서 언제든 켜고 끌 수 있어요"로 정리
- Playwright로 아이폰 화면에서 종 버튼 → 알림 받기 → 안내창이 맨 위에 뜨는 것 확인
- 서학동성당(project2)의 알림 버튼을 이 방식으로 통일하는 작업 중에 발견함

---

## 앞으로 작업이 필요할 수 있는 부분 (제안 — 명시적 지시는 없었음, 확인 후 진행)

- [ ] **전체 일정 인쇄 기능을 서학동성당(project2) 쪽에도 이식할지 확인.** (`sibling-project-functional-parity` 메모리: 포팅 시 겉모습뿐 아니라 JS 로직·버그 수정까지 동일하게 맞출 것)
- [ ] 실제 프린터/다양한 브라우저(엣지, 사파리 등)에서 "전체 일정 보기" 인쇄 결과 재검증 — 지금까지는 크롬 기준으로 여러 차례 수정됨
- [ ] 오늘의 말씀 자동 갱신 스크립트가 앞으로도 매일 정상 실행되는지 주기적으로 확인 (2026-09-26 한글 자모 절 표기로 한 차례 실패 → 수정 완료)
- [ ] 새로 올라가는 공지/팝업/본당 일정 텍스트는 게시 전 한글 맞춤법·띄어쓰기 자동 점검 (메모리 `proofread-korean-text-before-posting` 적용)
- [x] GitHub 토큰(`yongmeori-worker`, 이 저장소 Contents 읽기/쓰기만) 만들어 Worker 비밀값 `GITHUB_TOKEN`에 넣기 — 2026-09-26 사용자와 함께 완료
- [x] Workers 자동 배포 연결 (main, Deploy command `npx wrangler deploy`) — 2026-09-26 완료. D1 표 구조 변경은 `npm run db:migrate:remote`로 따로 적용
- [ ] **사용자와 함께: 카카오 개발자 사이트에 새 도메인 등록** — 등록 전까지 지도 칸이 회색 (docs C항목)
- [ ] 실사용 확인(구글 로그인·사진 올리기·자동 배포) 후 정리: GitHub Pages 끄기, 예전 Cloudflare Pages `yongmeori-catholic.pages.dev` 삭제, Firebase 승인 도메인에서 `pages.dev`·`github.io` 제거, 예전 classic 토큰 `yongmeori admin` 삭제
- [ ] 홈 화면에 예전 주소(github.io)로 설치한 사람이 있으면 새 주소로 다시 설치해야 함 (운영 전이라 영향 적음)
- [ ] 완료된 변경 사항은 항상 자동으로 커밋 + 푸시 (메모리 `always-commit-and-push`)

> 위 "앞으로" 항목은 최근 작업 흐름과 저장된 메모리를 바탕으로 한 제안입니다. 실제 우선순위는 사용자가 다음에 요청하는 작업에 맞춰 갱신해주세요.
